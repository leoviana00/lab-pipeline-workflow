apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-artifacts
  namespace: jenkins
spec:
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
